package OOP_PRINCIP.homeWork.task12;
/*
void processShape(Shape shape) {
    if (shape instanceof Circle) {
        Circle c = (Circle) shape;
        // обработка круга...
    } else if (shape instanceof Rectangle) {
        Rectangle r = (Rectangle) shape;
        // обработка прямоугольника...
    } else if (shape instanceof Triangle) {
        // обработка треугольника...
    }
}

 */
/* при добавлении в этот код фигуры мы должны каждый раз его изменять,нет полиморфизма
3)создаем интерфейс shape
каждая фигура сама себя определяет
class Circle implements Shape {
    public void process() {
        // обработка круга
    }

}

 */